{
  "name": "@ourpass/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "subgraph:auth": "yarn workspace @ourpass/subgraph auth",
    "subgraph:codegen": "yarn workspace @ourpass/subgraph codegen",
    "subgraph:build": "yarn workspace @ourpass/subgraph build",
    "subgraph:deploy": "yarn workspace @ourpass/subgraph deploy",
    "react-app:build": "yarn workspace @ourpass/react-app build",
    "react-app:eject": "yarn workspace @ourpass/react-app eject",
    "react-app:ipfs": "yarn workspace @ourpass/react-app ipfs",
    "react-app:start": "yarn workspace @ourpass/react-app start",
    "react-app:test": "yarn workspace @ourpass/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}